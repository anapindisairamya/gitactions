name: Super - Linter

on: push

jobs:
 super lint:
    name: Lint code base 
    uses: actions/checkout@v2

    Run: Super - Linter
    uses: github/super-linter@v3

    env: 
     DEFAULT_BRANCH: main
     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
     
